// Esta es la puerta de entrada: reexporta normalizarTitulo y PRIORIDADES, y exporta crearTarea.
// Los módulos titulos.js y prioridades.js ya están hechos; impórtalos desde aquí.

/**
 * Crea una tarea con el título normalizado y una prioridad válida.
 * @param {string} titulo
 * @param {string} [prioridad] 'baja', 'media' o 'alta'. Si no viene, usa la prioridad por defecto.
 * @returns {{ titulo: string, prioridad: string }}
 * @throws {Error} 'Prioridad inválida: <prioridad>' si no es una de PRIORIDADES.
 */
export function crearTarea(titulo, prioridad) {
  throw new Error('Ejercicio sin resolver: completa esta función');
}
