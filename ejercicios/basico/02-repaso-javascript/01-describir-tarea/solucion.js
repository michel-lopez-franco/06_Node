// Por ahora escribe solo dentro de la función; `export` lo veremos en el módulo 3.

/**
 * Describe una tarea en una línea de texto.
 * @param {{ titulo: string, prioridad?: string, etiquetas?: string[] }} tarea
 * @returns {string} Por ejemplo, 'Pagar luz [alta] #casa #urgente'.
 */
export function describirTarea(tarea) {
  throw new Error('Ejercicio sin resolver: completa esta función');
}
